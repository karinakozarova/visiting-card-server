from django.db import models

# Create your models here.

# Keep in mind we are using MongoDB
# Design these model classes which are best fit for NoSQL database


class user(models): pass
# create required entities here


class card(models): pass
# create required entities here


class dropped_card(models): pass
# create required entities here
